{
  "name": "chaos-socket",
  "version": "1.0.0",
  "description": "WebSocket server simulator with chaos engineering capabilities for load testing",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --no-cache",
    "test:watch": "jest --watch",
    "lint": "echo \"No linter configured\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "js-yaml": "^4.1.0",
    "winston": "^3.11.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
